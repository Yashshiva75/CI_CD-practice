version: "3.9"

services:
  backend:
    build: .
    container_name: node-backend
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/node_modules
    command: ["npm", "run", "dev"]
